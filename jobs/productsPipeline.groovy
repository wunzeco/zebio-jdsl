/*
    CI Pipeline - BUILD, DEPLOY, TEST
*/

/*
deliveryPipelineView('Products-CI-pipeline') {
    pipelineInstances(0)
    showAggregatedPipeline()
    showDescription()
    showTotalBuildTime()
    columns(1)
    sorting(Sorting.TITLE)
    updateInterval(5)
    enableManualTriggers()
    showAvatars()
    showChangeLog()
    pipelines {
        regex(/^(.*-build)$/)
    }
}
*/
