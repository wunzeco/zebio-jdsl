/*
    Infra Code Pipeline - VALIDATE, UPDATE
*/

/*
deliveryPipelineView('Infra-pipeline') {
    pipelineInstances(0)
    showAggregatedPipeline()
    showDescription()
    showTotalBuildTime()
    columns(1)
    sorting(Sorting.TITLE)
    updateInterval(5)
    enableManualTriggers()
    showAvatars()
    showChangeLog()
    pipelines {
        regex(/^(.*-validate)$/)
    }
}
*/
